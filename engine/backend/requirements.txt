# Install biotuner from parent directory (local development package)
-e ../..

# FastAPI and server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0
pydantic==2.5.0

# Core data processing
numpy>=1.21.4,<2.0
scipy>=1.7.3
pandas>=1.3.0
scikit-learn>=1.0.0

# Audio processing
librosa>=0.9.2
soundfile>=0.12.1

# Biotuner dependencies (already included via -e ../..)
# matplotlib, seaborn, mne, fooof, emd, contfrac, mido, sympy
# These are installed as part of the biotuner package
